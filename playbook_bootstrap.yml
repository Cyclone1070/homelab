---
- name: Bootstrap fresh machine
  hosts: all
  roles:
    - role: bootstrap
      tags: bootstrap

  post_tasks:
    - name: Run final state reconciliation
      ansible.builtin.include_role:
        name: cleanup
        apply:
          tags: [always, cleanup]
      tags: [always, cleanup]
