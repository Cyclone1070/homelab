---
- name: Bootstrap fresh machine
  hosts: all
  roles:
    - role: bootstrap
      tags: bootstrap

  post_tasks:
    - name: Run final state reconciliation
      include_role:
        name: cleanup
      tags: [always, cleanup]
